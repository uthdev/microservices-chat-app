import React from 'react'

import { Button, Intent } from '@blueprintjs/core'

const Root = () => {
  return (
    <div>
      <Button intent={Intent.SUCCESS} text="Click Me"/>
    </div>
  )
}

export default Root
